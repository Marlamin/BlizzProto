package BNET.BI;

message AgentUninstall {
	enum UninstallType {
		UNINSTALLTYPE_START = 0;
		UNINSTALLTYPE_COMPLETE = 1;
	}

	optional uint64 session_id = 1;
	optional uint64 timestamp = 2;
	optional string uid = 3;
	optional .BNET.BI.AgentUninstall.UninstallType uninstall_type = 4 [default = UNINSTALLTYPE_START];
	optional string local_version = 5;
}

